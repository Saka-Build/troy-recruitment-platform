spring:
  application:
    name: troy-ats

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 12MB

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  data:
    redis:
      host: localhost
      port: 6379

  profiles:
    active: database

  datasource:
    url: jdbc:postgresql://localhost:5432/troy_ats
    username: ats_admin
    password: TroyAts_&1947
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC
    open-in-view: false

  security:
    user:
      name: sakaadmin
      password: admin123

server:
  port: 8080

file:
  upload-original-cv-dir: C:/work/Docs-Troy-ats/upload/originalcv
  upload-troy-cv-dir: C:/work/Docs-Troy-ats/upload/troycv
  upload-photo-dir: C:/work/Docs-Troy-ats/upload/employee/photo

app:
  jwt:
    secret: "super-longest-secret-key-troyats"
    access-ttl-minutes: 30
    refresh-ttl-days: 2

